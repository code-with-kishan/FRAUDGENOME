version: '3.7'
services:
  api:
    image: python:3.10-slim
    working_dir: /app
    volumes:
      - ./:/app
    command: uvicorn api.app:app --host 0.0.0.0 --port 8000
    ports:
      - '8000:8000'
